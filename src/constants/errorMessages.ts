const ENTITY_ALREADY_EXISTS = 'Entity with this data already exists.';
const INVALID_CREDENTIALS = 'Invalid email or password.';
const FORBIDDEN = 'Forbidden.';

export default {
  entityAlreadyExists: ENTITY_ALREADY_EXISTS,
  invelidCredentials: INVALID_CREDENTIALS,
  forbidden: FORBIDDEN,
};
